{% extends 'base.html.twig' %}

{% block title %}Revenue{% endblock %}

{% block body %}
    <div class="section profile">
        <div class="tab-pane fade show active profile-overview" id="profile-overview">
            <div class="row">
                <div class="col-lg-3 col-md-4 label ">Descrição</div>
                <div class="col-lg-9 col-md-8">{{ revenue.description }}</div>
            </div>
            <div class="row">
                <div class="col-lg-3 col-md-4 label ">Categoria</div>
                <div class="col-lg-9 col-md-8">{{ category.description }}</div>
            </div>
            <div class="row">
                <div class="col-lg-3 col-md-4 label ">Conta</div>
                <div class="col-lg-9 col-md-8">{{ account.description }}</div>
            </div>
            <div class="row">
                <div class="col-lg-3 col-md-4 label ">Valor</div>
                <div class="col-lg-9 col-md-8">{{ revenue.amount|format_currency('BRL') }}</div>
            </div>
            <div class="row">
                <div class="col-lg-3 col-md-4 label ">Vencimento</div>
                <div class="col-lg-9 col-md-8">{{ revenue.date ? revenue.date|date('d/m/Y') : '' }}</div>
            </div>
            <div class="row">
                <div class="col-lg-3 col-md-4 label ">Repetição</div>
                <div class="col-lg-9 col-md-8">{{ revenue.repetitionType }}</div>
            </div>
            <div class="row">
                <div class="col-lg-3 col-md-4 label ">Periodicidade</div>
                <div class="col-lg-9 col-md-8">{{ revenue.repetitionPeriodicity }}</div>
            </div>
            <div class="row">
                <div class="col-lg-3 col-md-4 label ">Número de parcelas</div>
                <div class="col-lg-9 col-md-8">{{ revenue.repetitionInstallment }}</div>
            </div>
            <div class="row">
                <div class="col-lg-3 col-md-4 label ">Anotações</div>
                <div class="col-lg-9 col-md-8">{{ revenue.note }}</div>
            </div>

            <div class="text-center">
                <button type="button" class="btn btn-primary">
                    <a style="text-decoration: none; color: #FFF" href="{{ path('app_revenue_edit', {'id': revenue.id}) }}">Editar</a>
                </button>
                <button type="button" class="btn btn-danger">
                    <a class="" style="text-decoration: none; color: #FFF" href="#" data-bs-toggle="modal" data-bs-target="#basicModal">Excluir</a>
                </button>
            </div>
        </div>
    </div>

    <div class="modal fade" id="basicModal" tabindex="-1">
        <div class="modal-dialog">
            <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Excluir receita</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                Deseja realmente excluir a receita <b>{{ revenue.description }}</b> ?
            </div>
            <div class="modal-footer">
                <form method="post" action="{{ path('app_revenue_delete', {'id': revenue.id}) }}">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ revenue.id) }}">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Voltar</button>
                    <button type="submit" class="btn btn-danger">Excluir</button>
                </form>
            </div>
            </div>
        </div>
    </div><!-- End Basic Modal-->
{% endblock %}






