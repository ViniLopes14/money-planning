{% extends 'base.html.twig' %}

{% block title %}Receitas{% endblock %}

{% block body %}
    <div class="col-sm-12">
        <button type="button" class="btn btn-primary">
            <a style="text-decoration: none; color: #FFF" href="{{ path('app_expense_new') }}">Nova receita</a>
        </button>
    </div>
    <!-- Table with hoverable rows -->
    <table class="table table-hover">
        <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Descrição</th>
            <th scope="col">Valor</th>
            <th scope="col">Data</th>
            <th scope="col"></th>
        </tr>
        </thead>
        <tbody>
            {% for revenue in revenues %}
                <tr>
                    <td>{{ revenue.id }}</td>
                    <td>{{ revenue.description }}</td>
                    <td>{{ revenue.amount|format_currency('BRL') }}</td>
                    <td>{{ revenue.date ? revenue.date|date('d/m/Y') : '' }}</td>
                    <td>
                        <a class="ri-eye-fill" href="{{ path('app_revenue_show', {'id': revenue.id}) }}" data-bs-toggle="tooltip" data-bs-placement="top" data-bs-original-title="Visualizar"></a>
                        <a class="ri-pencil-fill" href="{{ path('app_revenue_edit', {'id': revenue.id}) }}" data-bs-toggle="tooltip" data-bs-placement="top" data-bs-original-title="Editar"></a>
                        <!--<a class="ri-close-circle-fill" href="#" data-bs-toggle="tooltip" data-bs-placement="top" data-bs-original-title="Excluir"></a>-->
                        {{ include('revenue/_delete_form.html.twig', {'id': revenue.id}) }}
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="12">Nenhum registro encontrado.</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
    <!-- End Table with hoverable rows -->
{% endblock %}
