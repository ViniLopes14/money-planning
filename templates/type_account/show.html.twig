{% extends 'base.html.twig' %}

{% block title %}Tipo de conta{% endblock %}

{% block body %}
    <div class="section profile">
        <div class="tab-pane fade show active profile-overview" id="profile-overview">
            <div class="row">
                <div class="col-lg-3 col-md-4 label ">Descrição</div>
                <div class="col-lg-9 col-md-8">{{ type_account.description }}</div>
            </div>
            
            <div class="text-center">
                <button type="button" class="btn btn-primary">
                    <a style="text-decoration: none; color: #FFF" href="{{ path('app_type_account_edit', {'id': type_account.id}) }}">Editar</a>
                </button>
                <button type="button" class="btn btn-danger">
                    <a class="" style="text-decoration: none; color: #FFF" href="#" data-bs-toggle="modal" data-bs-target="#basicModal">Excluir</a>
                </button>
            </div>
        </div>
    </div>

    <div class="modal fade" id="basicModal" tabindex="-1">
        <div class="modal-dialog">
            <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Excluir tipo de conta</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                Deseja realmente excluir o tipo de conta <b>{{ type_account.description }}</b> ?
            </div>
            <div class="modal-footer">
                <form method="post" action="{{ path('app_type_account_delete', {'id': type_account.id}) }}">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_account.id) }}">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Voltar</button>
                    <button type="submit" class="btn btn-danger">Excluir</button>
                </form>
            </div>
            </div>
        </div>
    </div><!-- End Basic Modal-->
{% endblock %}






